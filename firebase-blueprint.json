{
  "entities": {
    "PortalConfig": {
      "title": "PortalConfig",
      "description": "App development progress configuration settings",
      "type": "object",
      "properties": {
        "progressBar": {
          "type": "integer",
          "description": "Progress percentage from 1 to 100"
        },
        "controlCenterWeb": {
          "type": "string",
          "enum": ["Yet to Start", "Progressing", "Completed"]
        },
        "controlCenterWebProgress": {
          "type": "integer",
          "description": "Progress percentage from 0 to 100"
        },
        "sellerPortalWeb": {
          "type": "string",
          "enum": ["Yet to Start", "Progressing", "Completed"]
        },
        "sellerPortalWebProgress": {
          "type": "integer",
          "description": "Progress percentage from 0 to 100"
        },
        "mainAppAndroid": {
          "type": "string",
          "enum": ["Yet to Start", "Progressing", "Completed"]
        },
        "mainAppAndroidProgress": {
          "type": "integer",
          "description": "Progress percentage from 0 to 100"
        },
        "partnerAppAndroid": {
          "type": "string",
          "enum": ["Yet to Start", "Progressing", "Completed"]
        },
        "partnerAppAndroidProgress": {
          "type": "integer",
          "description": "Progress percentage from 0 to 100"
        },
        "estimatedDeliveryDate": {
          "type": "string",
          "description": "Estimated Delivery Date"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "progressBar",
        "controlCenterWeb",
        "sellerPortalWeb",
        "mainAppAndroid",
        "partnerAppAndroid",
        "estimatedDeliveryDate"
      ]
    }
  },
  "firestore": {
    "/portal/{configId}": {
      "schema": "PortalConfig",
      "description": "Portal development progress details document"
    }
  }
}
